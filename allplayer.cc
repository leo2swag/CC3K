#include "allplayer.h"
using namespace std;

drow::drow(int hp, int attack, int def, int race, char symbol, int gold, int row, int col): Player{hp, attack, def, race, symbol, gold, row, col} {}

goblin::goblin(int hp, int attack, int def, int race, char symbol, int gold, int row, int col): Player{hp, attack, def, race, symbol, gold, row, col} {}

shade::shade(int hp, int attack, int def, int race, char symbol, int gold, int row, int col): Player{hp, attack, def, race, symbol, gold, row, col} {}

troll::troll(int hp, int attack, int def, int race, char symbol, int gold, int row, int col): Player{hp, attack, def, race, symbol, gold, row, col} {}

vampire::vampire(int hp, int attack, int def, int race, char symbol, int gold, int row, int col): Player{hp, attack, def, race, symbol, gold, row, col} {}

hedis::hedis(int hp, int attack, int def, int race, char symbol, int gold, int row, int col): Player{hp, attack, def, race, symbol, gold, row, col} {}

aatrox::aatrox(int hp, int attack, int def, int race, char symbol, int gold, int row, int col): Player{hp, attack, def, race, symbol, gold, row, col} {}

god::god(int hp, int attack, int def, int race, char symbol, int gold, int row, int col): Player{hp, attack, def, race, symbol, gold, row, col} {}
